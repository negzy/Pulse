DATABASE_URL="file:./dev.db"

# Skool API (SkoolAPI.com – https://skoolapi.com/dashboard)
# Get your API key from the dashboard; required for Connect Skool.
SKOOL_API_SECRET="your_api_secret_here"
# Optional: used to encrypt stored Skool password for "Scrape now". If unset, SKOOL_API_SECRET is used.
# ENCRYPTION_KEY="random-secret-at-least-32-chars"
# Optional: override API base URL (default https://api.skoolapi.com)
# SKOOL_API_BASE_URL="https://api.skoolapi.com"

# Optional: for auto-fill posts/comments via scraping (Scrape now + daily script)
# Set these to run "Scrape now" without entering credentials, or for npm run scrape (cron).
# SKOOL_SCRAPE_EMAIL="your@skool-account.com"
# SKOOL_SCRAPE_PASSWORD="your-password"
